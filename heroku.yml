build:
  config:
    BUILD_ENV: production
  docker:
    web: Dockerfile
run:
  web: /start-server.sh
  worker:
    command:
      - python manage.py rqworker default
    image: web
release:
  command:
    - python manage.py migrate --noinput
  image: web
