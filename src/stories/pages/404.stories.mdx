import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { linkTo } from '@storybook/addon-links';

import { FourOhFour } from './404.stories';

<Meta title="Pages/404/Intro" component={FourOhFour} />

# 404

A page view also known as an “error page” or “Page Not Found” page. This page
indicates that the user reached the domain they requested, but the URL path
provided no information.

- If a custom message is not passed, then the default message is displayed with
  a link to the home page.

<Story id="pages-404-component--four-oh-four" />
