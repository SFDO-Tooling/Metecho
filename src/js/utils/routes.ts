/* eslint-disable @typescript-eslint/camelcase */

const routes = {
  home: () => '/',
};

export const routePatterns = {
  home: () => '/',
  auth_error: () => '/accounts/*',
};

export default routes;
